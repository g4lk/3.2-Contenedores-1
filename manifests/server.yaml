apiVersion: apps/v1
kind: Deployment
metadata:
  name: server
spec:
  selector:
    matchLabels:
      app: server
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: server
    spec:
      containers:
      - env:
        - name: MYSQL_DB
          value: eoloplantsDB
        - name: MYSQL_HOST
          value: mysql
        - name: MYSQL_PASS
          value: password
        - name: MYSQL_USER
          value: root
        - name: RABBITMQ_URL
          value: amqp://root:password@rabbitmq
        image: oscarsotosanchez/server:v1.0
        name: server
---

## service
apiVersion: v1
kind: Service
metadata:
  name: server
  labels:
    app: server
spec:
  ports:
  - port: 3000
  selector:
    app: server
  type: LoadBalancer